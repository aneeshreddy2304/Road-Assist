"use client";
import {useState,useEffect} from 'react';import {MapPin,CarFront,Wrench,CalendarDays,ShoppingBag,MessageCircle,Leaf,LayoutDashboard,Package,Users,ShieldCheck,BarChart3,Bell,ArrowUpRight,LogOut,ChevronDown,Menu,Settings,ClipboardList} from 'lucide-react';import type {Role} from '@/lib/demo-data';import Owner from '@/components/owner-prototype';import Operations from '@/components/operations-prototype';import {Modal,Toast} from '@/components/prototype-shared';import './prototype.css';import './pearl.css';
const navs={owner:[['Find help',MapPin],['My requests',Wrench],['My garage',CarFront],['Vehicle Care',Leaf],['Appointments',CalendarDays],['Parts shop',ShoppingBag],['Messages',MessageCircle],['Profile',Settings]],mechanic:[['Overview',LayoutDashboard],['Jobs',Wrench],['Appointments',CalendarDays],['Inventory',Package],['Parts marketplace',ShoppingBag],['Orders',ClipboardList],['Messages',MessageCircle],['Profile',Settings]],warehouse:[['Overview',LayoutDashboard],['Inventory',Package],['Orders',ClipboardList],['Messages',MessageCircle],['Profile',Settings]],admin:[['Overview',LayoutDashboard],['Users',Users],['Approvals',ShieldCheck],['Requests',Wrench],['Analytics',BarChart3],['Alerts',Bell]]} as const;
export default function Workspace({role:providedRole='owner',initialTab,userName,onLogout}:{role?:Role;initialTab?:string;userName?:string;onLogout?:()=>void}){const role:Role=['owner','mechanic','warehouse','admin'].includes(providedRole)?providedRole:'owner';const[tab,setTab]=useState(initialTab||(role==='owner'?'Find help':'Overview'));const[toast,setToast]=useState('');const[mobile,setMobile]=useState(false);const[modal,setModal]=useState('');const[paused,setPaused]=useState(false);const fallbackName=role==='owner'?'Jordan Ellis':role==='mechanic'?'Alex Morgan':role==='warehouse'?'Pacific Parts Co.':'Casey Brooks';const name=userName||fallbackName;const initials=name.split(/\s+/).map(part=>part[0]).join('').slice(0,2).toUpperCase();const items=navs[role];useEffect(()=>{document.documentElement.dataset.palette='pearl';return()=>{delete document.documentElement.dataset.palette}},[]);useEffect(()=>{document.title=`${tab} · Wingman`},[tab]);function go(s:string){setTab(s);setMobile(false);window.scrollTo({top:0,behavior:"instant"})}return <div className={"workspace "+(role==='owner'?'owner-workspace':'')}><a href="#workspace-content" className="skip-link">Skip to workspace</a><aside className={'sidebar '+(mobile?'mobile-open':'')}><a className="brand" href="/"><span className="brand-symbol">w<span>↗</span></span>wingman.</a><button className="workspace-selector" onClick={()=>setModal('switch')}><span className="workspace-role-icon">{role==='owner'?<CarFront size={18}/>:role==='warehouse'?<Package size={18}/>:role==='admin'?<ShieldCheck size={18}/>:<Wrench size={18}/>}</span><div><small>YOUR WORKSPACE</small><strong>{role==='owner'?'Vehicle owner':role==='admin'?'Administrator':role==='mechanic'?'Mechanic':'Warehouse'}</strong></div><ChevronDown size={15}/></button><span className="nav-caption">{role==='owner'?'YOUR ROAD COMPANION':'YOUR WORKSPACE'}</span><nav className="side-nav">{items.map(([label,Icon])=><button key={label} aria-current={tab===label?"page":undefined} className={tab===label?'active':''} onClick={()=>go(label)}><Icon size={19} strokeWidth={1.7}/><span>{label}</span>{label==='My requests'&&<small>1</small>}{label==='Messages'&&<span className="nav-dot"/>}{label==='Approvals'&&<small>2</small>}</button>)}</nav><div className="sidebar-bottom"><button className="sidebar-user" onClick={()=>setModal('account')}><span className="avatar">{initials}</span><div><strong>{name}</strong><small>Wingman account</small></div><ChevronDown size={15}/></button></div></aside>{mobile&&<button className="sidebar-backdrop" aria-label="Close navigation" onClick={()=>setMobile(false)}/>} 
<div className="workspace-main"><header className="workspace-topbar"><button className="mobile-menu icon-button" aria-label="Open navigation" onClick={()=>setMobile(!mobile)}><Menu size={21}/></button><div className="breadcrumb"><span>Workspace</span><ChevronDown size={11} className="breadcrumb-chevron"/><strong>{tab}</strong></div><div className="topbar-actions"><span className="workspace-date">Thursday, September 17</span><button className="prototype-badge" onClick={()=>setModal('about')}><span/>Visual prototype</button><button className="notification-button icon-button" aria-label="View notifications" onClick={()=>setModal('notifications')}><Bell size={20}/><i/></button><button className="avatar" aria-label="Your account" onClick={()=>setModal('account')}>{initials}</button></div></header><main className="workspace-content" id="workspace-content">{role==='owner'?<Owner tab={tab} setTab={go} notify={setToast}/>:<Operations key={role} role={role} tab={tab} setTab={go} notify={setToast}/>}</main><footer className={'workspace-disclaimer '+(paused?'paused':'')}><span>DEMO DIRECTORY</span><div>Sample businesses, availability, stock, prices, and activity. No live provider participation or roadside dispatch.</div><button onClick={()=>setPaused(!paused)} aria-label={paused?'Resume disclaimer':'Pause disclaimer'}>{paused?'Resume':'Pause'}</button></footer></div>
{toast&&<Toast text={toast} onClose={()=>setToast('')}/>}{modal==='switch'&&<Modal title="One network. Four perspectives." subtitle="Explore how Wingman works for everyone involved." onClose={()=>setModal('')}><div className="switch-roles">{(['owner','mechanic','warehouse','admin'] as Role[]).map(r=><a href={'/demo/'+r} key={r}><span className="provider-avatar">{r==='owner'?<CarFront size={21}/>:r==='mechanic'?<Wrench size={21}/>:r==='warehouse'?<Package size={21}/>:<ShieldCheck size={21}/>}</span><div><strong>{r==='admin'?'Administrator':r.charAt(0).toUpperCase()+r.slice(1)}</strong><small>{r==='owner'?'Find help and care for your vehicle':r==='mechanic'?'Manage jobs, parts, and appointments':r==='warehouse'?'Keep inventory and orders moving':'Oversee the network and its people'}</small></div><ArrowUpRight size={18}/></a>)}</div></Modal>}{modal==='notifications'&&<Modal title="A little heads-up." subtitle="Your sample in-app notifications." onClose={()=>setModal('')}><div className="notification-list">{(role==='owner'?[['Your next check-in is October 1','We’ll remind you at 9:00 AM Pacific.','Vehicle Care'],['Alex has an update for you','Battery replacement is in progress.','Messages'],['Your service is coming up','Oil change · September 22 at 9:30 AM','Appointments']]:[['A little attention needed','Review today’s open work and requests.','Overview'],['Inventory is running low','Two essentials could use a restock.','Inventory']]).map(([title,text,destination])=><button key={title} onClick={()=>{go(role==='admin'?'Alerts':destination);setModal('')}}><span className="icon-disc"><Bell size={18}/></span><div><strong>{title}</strong><p>{text}</p></div><ArrowUpRight size={16}/></button>)}</div></Modal>}{modal==='account'&&<Modal title={name} subtitle="Your Wingman account" onClose={()=>setModal('')}><div className="account-actions">{role==='owner'&&<button className="secondary" onClick={()=>{go('Profile');setModal('')}}>Edit profile</button>}<button className="secondary" onClick={()=>setModal('switch')}>Switch workspace <ArrowUpRight size={17}/></button><button className="secondary" onClick={()=>{onLogout?.();window.location.assign('/login')}}>Back to sign in <LogOut size={17}/></button><a className="text-button" href="/">Revisit the Wingman story <ArrowUpRight size={16}/></a></div></Modal>}{modal==='about'&&<Modal title="About Wingman" subtitle="One connected roadside workspace" onClose={()=>setModal('')}><p>Explore the owner, mechanic, warehouse, and administrator experiences in one connected system.</p><button className="primary" onClick={()=>setModal('switch')}>Explore workspaces <ArrowUpRight size={17}/></button></Modal>}</div>}
