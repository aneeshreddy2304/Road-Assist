"use client";
import {useState,useEffect} from 'react';import {ArrowUpRight,ArrowRight,Plus,Search,Package,Truck,Wrench,Clock,Check,CheckCircle2,AlertTriangle,TrendingUp,Users,CalendarDays,MapPin,ChevronRight,Send,ShieldCheck,X,Activity,ShoppingBag,Download} from 'lucide-react';import {PageTitle,Stat,Badge,Modal,Empty,ProductArt} from './prototype-shared';import {initialRequests,initialInventory,initialOrders,products,initialAppointments,type Role} from '@/lib/demo-data';
import OwnerMessages from './owner-messages';import BusinessProfile from './business-profile';import WarehouseMarketplace from './warehouse-marketplace';
export default function Operations({role,tab,setTab,notify}:{role:Role;tab:string;setTab:(t:string)=>void;notify:(s:string)=>void}){const[available,setAvailable]=useState(true);const[jobs,setJobs]=useState([...initialRequests,{id:'WM-2049',provider:'Sunset Auto Care',issue:'Flat tire near Golden Gate Park',vehicle:'2020 Honda Civic',status:'Requested',date:'8 minutes ago',mode:'Mobile service',mechanic:'Unassigned',notes:'Front passenger tire is flat. Parked safely.'}]);const[inventory,setInventory]=useState(initialInventory);const[orders,setOrders]=useState(initialOrders.map(o=>({...o,warehouse:'Pacific Parts Co.'})));const[modal,setModal]=useState('');const[billingJob,setBillingJob]=useState<(typeof jobs)[number]|null>(null);const[orderId,setOrderId]=useState('PO-1084');const[edit,setEdit]=useState(initialInventory[0]);const[search,setSearch]=useState('');const[filter,setFilter]=useState('All');const[alerts,setAlerts]=useState([{id:1,title:'AGM battery stock is running low',text:'Sunset Auto Care · 3 units remaining',severity:'Inventory'},{id:2,title:'A provider is waiting for approval',text:'Coastline Auto Service · Submitted today',severity:'Registration'},{id:3,title:'Request awaiting assignment',text:'WM-2049 · Waiting for 8 minutes',severity:'Request'}]);const[accounts,setAccounts]=useState([{id:1,name:'Jordan Ellis',email:'jordan@wingman.example',role:'Owner',status:'Active',initials:'JE'},{id:2,name:'Alex Morgan',email:'alex@wingman.example',role:'Mechanic',status:'Active',initials:'AM'},{id:3,name:'Pacific Parts Co.',email:'parts@wingman.example',role:'Warehouse',status:'Active',initials:'PP'},{id:4,name:'Coastline Auto Service',email:'coastline@wingman.example',role:'Mechanic',status:'Pending',initials:'CA'},{id:5,name:'Valley Parts Supply',email:'valley@wingman.example',role:'Warehouse',status:'Pending',initials:'VP'}]);const[chat,setChat]=useState(['Hi Alex, your order PO-1084 is packed and ready for the next delivery run.','Perfect, thank you. Please keep the oil filters together.']);const[draft,setDraft]=useState('');
useEffect(()=>{setFilter('All');setSearch('')},[tab]);
const isAdmin=role==='admin',isWarehouse=role==='warehouse';const goJobs=()=>setTab(isWarehouse?'Orders':'Jobs');function nextOrder(id:string){setOrders(orders.map(o=>o.id===id?{...o,status:['Placed','Confirmed','Packed','Shipped','Delivered'][Math.min(4,['Placed','Confirmed','Packed','Shipped','Delivered'].indexOf(o.status)+1)]}:o));notify('Sample order status updated')}
return <>
{tab==='Overview'&&<><PageTitle title="Overview" action={isAdmin?<Badge>California · Demonstration</Badge>:<button className={'availability-toggle '+(available?'on':'')} onClick={()=>setAvailable(!available)}><span/>{available?'Accepting new '+(isWarehouse?'orders':'jobs'):'Currently unavailable'}</button>}/><div className="stat-grid">{isAdmin?<><Stat label="Total accounts" value="248" change="Across 4 role workspaces" icon={<Users size={19}/>}/><Stat label="Active requests" value="18" change="6 currently in progress" icon={<Wrench size={19}/>}/><Stat label="Provider approvals" value={String(accounts.filter(a=>a.status==='Pending').length)} change="Ready for your review" icon={<ShieldCheck size={19}/>}/><Stat label="Open alerts" value={String(alerts.length)} change="Keep the network healthy" icon={<AlertTriangle size={19}/>}/></>:isWarehouse?<><Stat label="Open orders" value={String(orders.filter(o=>o.status!=='Delivered').length)} change="Ready to keep moving" icon={<ShoppingBag size={19}/>}/><Stat label="Products in stock" value="126" change="Across 8 categories" icon={<Package size={19}/>}/><Stat label="Low-stock items" value={String(inventory.filter(p=>p.quantity<5).length)} change="A little restock needed" icon={<AlertTriangle size={19}/>}/><Stat label="Delivered this week" value="24" change="8 more than last week" icon={<Truck size={19}/>}/></>:<><Stat label="Today’s jobs" value="4" change="2 completed · 1 in progress" icon={<Wrench size={19}/>}/><Stat label="New requests" value={String(jobs.filter(j=>j.status==='Requested').length)} change="One more person to help" icon={<MapPin size={19}/>}/><Stat label="Upcoming visits" value="3" change="Next at 1:30 PM" icon={<CalendarDays size={19}/>}/><Stat label="Low-stock parts" value={String(inventory.filter(p=>p.quantity<5).length)} change="Keep your shelf ready" icon={<Package size={19}/>}/></>}</div><div className="operations-layout"><section><div className="panel section-panel"><div className="section-heading"><div><h2>{isAdmin?'Activity across the network':isWarehouse?'Orders in motion':'Your work, in motion'}</h2></div><button className="text-button" onClick={()=>isAdmin?setTab('Analytics'):goJobs()}>View all <ArrowUpRight size={15}/></button></div>{isAdmin?<div className="activity-chart"><div className="chart-legend"><span/>Service requests <i/>Completed jobs</div><div className="chart-bars">{[42,65,51,78,58,89,72].map((n,i)=><div key={i}><div className="bar-pair"><span style={{height:n+'%'}}/><span style={{height:(n-15)+'%'}}/></div><small>{['Fri','Sat','Sun','Mon','Tue','Wed','Thu'][i]}</small></div>)}</div></div>:isWarehouse?<div className="table-wrap"><table><thead><tr><th>ORDER</th><th>MECHANIC</th><th>STATUS</th><th>TOTAL</th></tr></thead><tbody>{orders.map(o=><tr key={o.id} onClick={()=>{setOrderId(o.id);setModal('order-detail')}} className="click-row"><td><strong>{o.id}</strong><small>{o.date}</small></td><td>{o.buyer}</td><td><Badge tone={o.status==='Delivered'?'gray':'green'}>{o.status}</Badge></td><td>${o.total.toFixed(2)}</td></tr>)}</tbody></table></div>:jobs.filter(j=>j.status!=='Completed').map(j=><div className="work-item" key={j.id}><span className="work-icon"><Wrench size={20}/></span><div><div className="between"><h3>{j.issue}</h3><Badge tone={j.status==='Requested'?'amber':'green'}>{j.status}</Badge></div><p>{j.vehicle} · {j.mode}</p><small>{j.id} · {j.date}</small></div><button className="icon-button" aria-label="View job" onClick={()=>setTab('Jobs')}><ArrowUpRight size={20}/></button></div>)}</div><div className="panel section-panel"><div className="section-heading"><div><h2>{isAdmin?'Recent network activity':isWarehouse?'Your delivery rhythm':'Coming up today'}</h2></div><Clock size={20}/></div>{(isAdmin?[['10:42 AM','Request WM-2048 moved to in progress','Sunset Auto Care · San Francisco'],['10:30 AM','Parts order PO-1084 packed','Pacific Parts Co. · Oakland'],['10:18 AM','New provider registration received','Coastline Auto Service · Santa Cruz']]:isWarehouse?[['10:00 AM','Morning orders packed','4 orders · Bay Area delivery route'],['1:30 PM','Afternoon delivery pickup','3 orders · San Francisco + Oakland'],['4:00 PM','Inventory reconciliation','Review low-stock essentials']]:[['11:30 AM','Finish battery replacement','Jordan Ellis · 2021 Toyota RAV4'],['1:30 PM','Oil change & safety check','Sam Rivera · 2020 Honda CR-V'],['3:00 PM','Brake inspection','Taylor Kim · 2019 Mazda CX-5']]).map(([time,title,desc])=><div className="schedule-row" key={time}><span>{time}</span><div><h3>{title}</h3><p>{desc}</p></div><span className="schedule-dot"/></div>)}</div></section><aside><div className="operations-banner"><span className="icon-disc">{isAdmin?<ShieldCheck size={24}/>:isWarehouse?<Truck size={24}/>:<Wrench size={24}/>}</span><h2>{isAdmin?'Provider approvals':'Low-stock parts'}</h2><button className="secondary full" onClick={()=>setTab(isAdmin?'Approvals':'Inventory')}>{isAdmin?'Review providers':'Review inventory'}<ArrowRight size={17}/></button></div><div className="panel section-panel"><div className="section-heading"><h3>{isAdmin?'Needs a little attention':'Shelf check'}</h3><span className="count-badge">{isAdmin?alerts.length:2}</span></div>{isAdmin?alerts.slice(0,3).map(a=><div className="mini-alert" key={a.id}><AlertTriangle size={17}/><div><strong>{a.title}</strong><small>{a.text}</small></div></div>):inventory.filter(p=>p.quantity<5).map(p=><div className="mini-alert" key={p.id}><Package size={18}/><div><strong>{p.name}</strong><small>{p.quantity} left · Restock recommended</small></div></div>)}</div></aside></div></>}
{tab==='Jobs'&&<><PageTitle title="Jobs" action={<Badge>{available?'Available for requests':'Not accepting requests'}</Badge>}/><div className="filter-row">{['All','Requested','Accepted','In progress','Completed'].map(s=><button key={s} className={'chip '+(filter===s?'selected':'')} onClick={()=>setFilter(s)}>{s}</button>)}</div><div className="request-grid">{jobs.filter(j=>filter==='All'||j.status===filter).map(j=><article className="panel request-card" key={j.id}><div className="between"><span className="overline">{j.id}</span><Badge tone={j.status==='Requested'?'amber':j.status==='Completed'?'gray':'green'}>{j.status}</Badge></div><h2>{j.issue}</h2><p className="muted">{j.vehicle}</p><div className="detail-box"><MapPin size={20}/><div><strong>{j.mode} · 1.2 miles away</strong><p>{j.notes}</p></div></div><div className="job-person"><span className="avatar">JE</span><div><strong>Jordan Ellis</strong><small>Vehicle owner · {j.date}</small></div></div><div className="button-row">{j.status!=='Completed'&&<button className="primary" onClick={()=>{if(j.status==='In progress'){setBillingJob(j);setModal('complete-job');return}setJobs(jobs.map(x=>x.id===j.id?{...x,status:j.status==='Requested'?'Accepted':'In progress'}:x));notify('Sample job status updated')}}>{j.status==='Requested'?'Accept request':j.status==='Accepted'?'Begin work':'Complete job'}<ArrowRight size={16}/></button>}<button className="secondary" onClick={()=>setTab('Messages')}>Message owner</button></div></article>)}</div></>}
{tab==='Inventory'&&<><PageTitle title="Inventory" action={<button className="primary" onClick={()=>setModal('add-product')}><Plus size={17}/> Add inventory</button>}/><div className="inventory-summary"><Badge>{inventory.length} products</Badge><Badge tone="amber">{inventory.filter(p=>p.quantity<5).length} running low</Badge><span>Illustrative stock levels</span></div><div className="panel"><div className="table-toolbar"><div className="search-input"><Search size={17}/><input placeholder="Find a part or SKU…" aria-label="Search inventory" value={search} onChange={e=>setSearch(e.target.value)}/></div><button className="secondary" onClick={()=>{setFilter(filter==='Low stock'?'All':'Low stock')}}>{filter==='Low stock'?'Show all inventory':'Low stock only'}</button></div><div className="table-wrap"><table><thead><tr><th>PRODUCT</th><th>SKU</th><th>CATEGORY</th><th>UNIT PRICE</th><th>IN STOCK</th><th>STATUS</th><th/></tr></thead><tbody>{inventory.filter(p=>(p.name.toLowerCase().includes(search.toLowerCase())||p.sku.toLowerCase().includes(search.toLowerCase()))&&(filter!=='Low stock'||p.quantity<5)).map(p=><tr key={p.id}><td><div className="table-product"><span className="table-product-art" style={{background:p.color}}><Package size={22}/></span><div><strong>{p.name}</strong><small>{p.unit}</small></div></div></td><td className="muted">{p.sku}</td><td>{p.category}</td><td>${p.price.toFixed(2)}</td><td><strong>{p.quantity}</strong> units</td><td><Badge tone={p.quantity<5?'amber':'green'}>{p.quantity<5?'Low stock':'In stock'}</Badge></td><td><button className="text-button" onClick={()=>{setEdit(p);setModal('edit-stock')}}>Update <ArrowUpRight size={14}/></button></td></tr>)}</tbody></table></div></div>{!isWarehouse&&<div className="care-banner"><Package size={30}/><div><h3>Warehouse parts</h3></div><button className="secondary" onClick={()=>setTab('Parts marketplace')}>Find parts <ArrowRight size={16}/></button></div>}</>}
{tab==='Orders'&&<><PageTitle title="Orders" action={!isWarehouse?<button className="primary" onClick={()=>setTab('Parts marketplace')}><Plus size={17}/> Order parts</button>:undefined}/><div className="filter-row">{['All','Placed','Confirmed','Packed','Shipped','Delivered'].map(s=><button className={'chip '+(filter===s?'selected':'')} key={s} onClick={()=>setFilter(s)}>{s}</button>)}</div><div className="panel table-wrap"><table><thead><tr><th>ORDER</th><th>{isWarehouse?'CUSTOMER':'WAREHOUSE'}</th><th>ITEMS</th><th>TOTAL</th><th>STATUS</th><th>NEXT STEP</th></tr></thead><tbody>{orders.filter(o=>filter==='All'||o.status===filter).map(o=><tr key={o.id}><td><strong>{o.id}</strong><small>{o.date}</small></td><td>{isWarehouse?o.buyer:o.warehouse}</td><td>{o.items}</td><td>${o.total.toFixed(2)}</td><td><Badge tone={o.status==='Delivered'?'gray':'green'}>{o.status}</Badge></td><td>{o.status==='Delivered'?<span className="muted">All delivered</span>:<button className="text-button" onClick={()=>isWarehouse?nextOrder(o.id):(setOrderId(o.id),setModal('order-detail'))}>{isWarehouse?({Placed:'Confirm order',Confirmed:'Mark packed',Packed:'Mark shipped',Shipped:'Mark delivered'} as Record<string,string>)[o.status]:'Track order'} <ArrowRight size={15}/></button>}</td></tr>)}</tbody></table></div></>}
<div hidden={tab!=='Parts marketplace'}><PageTitle title="Parts marketplace"/><WarehouseMarketplace onOrder={(warehouse,item,quantity,total)=>{setOrders([{id:'PO-'+Date.now().toString().slice(-6),buyer:'Sunset Auto Care',warehouse,items:`${item} × ${quantity}`,total,status:'Placed',date:'Just now'},...orders]);notify('Demo warehouse order placed');setTab('Orders')}}/></div>
{tab==='Appointments'&&<><PageTitle title="Appointments" /><div className="panel table-wrap"><table><thead><tr><th>WHEN</th><th>SERVICE</th><th>VEHICLE</th><th>OWNER</th><th>STATUS</th><th/></tr></thead><tbody>{initialAppointments.map(a=><tr key={a.id}><td><strong>{a.date}</strong><small>{a.time}</small></td><td>{a.service}</td><td>{a.vehicle}</td><td>Jordan Ellis</td><td><Badge>{a.status}</Badge></td><td><button className="text-button" onClick={()=>{setModal('appointment-detail')}}>Manage <ArrowUpRight size={15}/></button></td></tr>)}</tbody></table></div></>}
<div hidden={tab!=='Messages'}><PageTitle title="Messages"/><OwnerMessages role={role}/></div>
<div hidden={tab!=='Profile'}><PageTitle title="Business profile"/><BusinessProfile warehouse={isWarehouse} notify={notify}/></div>
{tab==='Approvals'&&<><PageTitle title="Approvals" /><div className="request-grid">{accounts.filter(a=>a.status==='Pending').map(a=><article className="panel approval-card" key={a.id}><div className="between"><span className="provider-avatar">{a.initials}</span><Badge tone="amber">Pending review</Badge></div><h2>{a.name}</h2><p>{a.role} · California</p><div className="detail-box"><ShieldCheck size={22}/><div><strong>Profile ready for review</strong><p>{a.email}<br/>Submitted September 17, 2026</p></div></div><div className="button-row"><button className="primary" onClick={()=>{setAccounts(accounts.map(x=>x.id===a.id?{...x,status:'Active'}:x));notify('Sample provider approved')}}><Check size={16}/> Approve</button><button className="secondary" onClick={()=>{setAccounts(accounts.map(x=>x.id===a.id?{...x,status:'Declined'}:x));notify('Sample registration declined')}}>Decline</button></div></article>)}{accounts.every(a=>a.status!=='Pending')&&<Empty title="All caught up" text="There are no more sample registrations to review."/>}</div></>}
{tab==='Users'&&<><PageTitle title="Users" /><div className="panel"><div className="table-toolbar"><div className="search-input"><Search size={17}/><input aria-label="Search users" placeholder="Search names or email…" value={search} onChange={e=>setSearch(e.target.value)}/></div><Badge>{accounts.length} sample accounts</Badge></div><div className="table-wrap"><table><thead><tr><th>ACCOUNT</th><th>EMAIL</th><th>ROLE</th><th>STATUS</th><th>ACTION</th></tr></thead><tbody>{accounts.filter(a=>(a.name+a.email).toLowerCase().includes(search.toLowerCase())).map(a=><tr key={a.id}><td><div className="table-product"><span className="avatar">{a.initials}</span><strong>{a.name}</strong></div></td><td>{a.email}</td><td>{a.role}</td><td><Badge tone={a.status==='Active'?'green':a.status==='Pending'?'amber':'gray'}>{a.status}</Badge></td><td><button className="text-button" onClick={()=>{setAccounts(accounts.map(x=>x.id===a.id?{...x,status:x.status==='Active'?'Inactive':'Active'}:x));notify('Sample account status updated')}}>{a.status==='Active'?'Deactivate':'Activate'}</button></td></tr>)}</tbody></table></div></div></>}
{tab==='Alerts'&&<><PageTitle title="Alerts" /><div className="panel section-panel">{alerts.map(a=><div className="alert-row" key={a.id}><span className="alert-icon"><AlertTriangle size={21}/></span><div><Badge tone="amber">{a.severity}</Badge><h3>{a.title}</h3><p>{a.text}</p></div><button className="secondary" onClick={()=>{setAlerts(alerts.filter(x=>x.id!==a.id));notify('Alert resolved')}}><Check size={16}/> Resolve</button></div>)}{!alerts.length&&<Empty title="A clear road ahead" text="All sample alerts have been resolved."/>}</div></>}
{tab==='Analytics'&&<><PageTitle title="Analytics" /><div className="stat-grid"><Stat label="Completed services" value="184" change="↑ 18% from previous month" icon={<CheckCircle2 size={19}/>}/><Stat label="Parts orders" value="96" change="↑ 12% from previous month" icon={<Package size={19}/>}/><Stat label="Average rating" value="4.8 / 5" change="From 142 sample reviews" icon={<TrendingUp size={19}/>}/><Stat label="Active providers" value="42" change="Across 5 categories" icon={<Users size={19}/>}/></div><div className="analytics-grid"><div className="panel section-panel"><div className="section-heading"><div><h2>Service activity</h2><p>Requests and completed services · Past 7 days</p></div><Badge>Sample data</Badge></div><div className="activity-chart"><div className="chart-legend"><span/>Requests <i/>Completed</div><div className="chart-bars">{[50,65,48,78,59,90,74].map((n,i)=><div key={i}><div className="bar-pair"><span style={{height:n+'%'}}/><span style={{height:(n-12)+'%'}}/></div><small>{['Fri','Sat','Sun','Mon','Tue','Wed','Thu'][i]}</small></div>)}</div></div></div><div className="panel section-panel"><h2>What brings people here</h2><p className="muted">Service mix · This month</p><div className="donut-chart"><div><strong>184</strong><span>services</span></div></div><div className="donut-legend">{[['Routine care','42%'],['Roadside help','28%'],['Tire service','18%'],['Other repairs','12%']].map(([s,n],i)=><div key={s}><span style={{background:['#244d3e','#8ca67b','#d8eb9d','#e8eade'][i]}}/>{s}<strong>{n}</strong></div>)}</div></div></div></>}
{tab==='Requests'&&<><PageTitle title="Requests" /><div className="panel table-wrap"><table><thead><tr><th>REQUEST</th><th>ISSUE</th><th>VEHICLE</th><th>PROVIDER</th><th>STATUS</th></tr></thead><tbody>{jobs.map(j=><tr key={j.id}><td><strong>{j.id}</strong><small>{j.date}</small></td><td>{j.issue}</td><td>{j.vehicle}</td><td>{j.provider}</td><td><Badge tone={j.status==='Requested'?'amber':'green'}>{j.status}</Badge></td></tr>)}</tbody></table></div></>}
{modal==='edit-stock'&&<Modal title="Keep your shelf up to date." subtitle={edit.name} onClose={()=>setModal('')}><form className="form-grid" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);setInventory(inventory.map(p=>p.id===edit.id?{...p,quantity:Number(d.get('quantity')),price:Number(d.get('price'))}:p));setModal('');notify('Stock updated')}}><label>Quantity on hand<input name="quantity" type="number" min={0} required defaultValue={edit.quantity}/></label><label>Unit price ($)<input name="price" type="number" min={0} step="0.01" required defaultValue={edit.price}/></label><button className="primary full">Save inventory <Check size={17}/></button></form></Modal>}
{modal==='add-product'&&<Modal title="Make room for a new essential." onClose={()=>setModal('')}><form className="form-grid" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);setInventory([...inventory,{...initialInventory[0],id:Date.now(),name:String(d.get('name')),sku:String(d.get('sku')),quantity:Number(d.get('quantity')),price:Number(d.get('price'))}]);setModal('');notify('Product added to sample inventory')}}><label>Product name<input name="name" required/></label><label>SKU<input name="sku" required/></label><div className="form-columns"><label>Quantity<input name="quantity" type="number" min={0} required/></label><label>Unit price ($)<input name="price" type="number" min={0} step="0.01" required/></label></div><button className="primary">Add inventory <Plus size={17}/></button></form></Modal>}
{modal==='order-detail'&&<Modal title={'Order '+orderId} subtitle={orders.find(o=>o.id===orderId)?.warehouse} onClose={()=>setModal('')}><p>{orders.find(o=>o.id===orderId)?.items}</p><Badge>{orders.find(o=>o.id===orderId)?.status}</Badge><p>Demo total: ${orders.find(o=>o.id===orderId)?.total.toFixed(2)}</p><button className="primary full" onClick={()=>{setModal('');setTab('Messages')}}>Messages <Send size={17}/></button></Modal>}
{modal==='appointment-detail'&&<Modal title="Oil change & inspection" subtitle="September 22 · 9:30 AM · Jordan Ellis" onClose={()=>setModal('')}><div className="detail-box"><CalendarDays size={24}/><div><strong>2021 Toyota RAV4</strong><p>Shop visit · Owner will bring the vehicle.</p></div></div><div className="button-row"><button className="primary" onClick={()=>{notify('Sample appointment confirmed');setModal('')}}>Confirm appointment <Check size={16}/></button><button className="secondary" onClick={()=>{setModal('');setTab('Messages')}}>Message owner</button></div></Modal>}
{modal==='complete-job'&&billingJob&&<Modal title="Complete job and record bill" subtitle={`${billingJob.id} · ${billingJob.vehicle}`} onClose={()=>{setModal('');setBillingJob(null)}}><form className="form-grid" onSubmit={event=>{event.preventDefault();const data=new FormData(event.currentTarget);const invoice={reference:`INV-${billingJob.id}`,requestId:billingJob.id,description:String(data.get('description')),labor:Number(data.get('labor')),parts:Number(data.get('parts')),fees:Number(data.get('fees')||0),note:String(data.get('note')||''),createdAt:new Date().toISOString()};const saved=JSON.parse(localStorage.getItem('wingman-recorded-invoices')||'[]');localStorage.setItem('wingman-recorded-invoices',JSON.stringify([invoice,...saved]));setJobs(jobs.map(job=>job.id===billingJob.id?{...job,status:'Completed',notes:`${job.notes} Final recorded bill: $${(invoice.labor+invoice.parts+invoice.fees).toFixed(2)}.`}:job));setModal('');setBillingJob(null);notify('Job completed and the recorded bill is available to the owner')}}><p className="muted">A recorded bill is required to complete the job. Wingman records it for the owner; no payment is processed.</p><label>Work completed<input name="description" required placeholder="Describe the completed repair"/></label><div className="form-columns"><label>Labor ($)<input name="labor" type="number" min="0" step="0.01" required/></label><label>Parts ($)<input name="parts" type="number" min="0" step="0.01" required/></label></div><label>Taxes and fees ($)<input name="fees" type="number" min="0" step="0.01" defaultValue="0"/></label><label>Owner note<textarea name="note" placeholder="Warranty, follow-up, or care instructions"/></label><button className="primary full">Record bill and complete job <Check size={17}/></button></form></Modal>}
</>}
